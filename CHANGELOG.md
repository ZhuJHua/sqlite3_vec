## 0.1.1

- State the bundled sqlite-vec version (v0.1.9) in the description and the README, and
  document how the two version lines relate. No code change.

## 0.1.0

- Initial release: the `vec0` virtual table and the `vec_*()` functions from sqlite-vec v0.1.9,
  compiled from the bundled C by a Dart build hook and shipped as a code asset.
